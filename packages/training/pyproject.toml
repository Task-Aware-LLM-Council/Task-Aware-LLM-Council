[project]
name = "training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Bhaskar Mahajan", email = "bhaskarmahajan98@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "common",
    "council-policies",
    "llm-gateway",
]

[project.optional-dependencies]
router = [
    "torch>=2.2",
    "transformers>=4.52.4",
    "datasets>=2.19",
    "scikit-learn>=1.3",
    "numpy>=1.26",
]
decomposer-router = [
    "datasets>=2.19",
    "huggingface-hub>=0.23",
]

[build-system]
requires = ["uv_build>=0.9.1,<0.10.0"]
build-backend = "uv_build"

[tool.uv.sources]
common = { workspace = true }
council-policies = { workspace = true }
llm-gateway = { workspace = true }
